[package]
name = "cobol-ir"
version = "0.1.0"
edition = "2021"
rust-version = "1.74"
license = "MIT OR Apache-2.0"
description = "Internal COBOL compiler IR for cobol-packed converter"

[dependencies]
cobol-record = { path = "../cobol-record" }
serde = { version = "1.0", features = ["derive"] }
